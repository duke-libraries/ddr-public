<%# "Document" items are currently multi-res, multi-image items
with additional file(s) as components, e.g., PDF with full-text %>
<%= render partial: 'display_multi_image', locals: {document: document} %>