<% cache("display_audio_#{params[:collection]}_#{params[:id]}", expires_in: 1.day) do %>
  <%= render partial: "item_options", locals: { derivatives: derivative_urls(document) } %>
  <%= render partial: "display_jwplayer", locals: { derivatives: derivative_urls(document), width: "100%", height: 30  } %>
<% end %>

<%= render partial: "show_default", locals: {document: document} %>

