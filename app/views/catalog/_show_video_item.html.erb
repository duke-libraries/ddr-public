<% cache("display_video_#{params[:collection]}_#{params[:id]}", expires_in: 1.day) do %>
  <%= render partial: "item_options", locals: { derivatives: derivative_urls(document) } %>
  <%= render partial: "display_jwplayer", locals: { derivatives: derivative_urls(document), width: "100%", aspectratio: "4:3"  } %>
  <%# TODO: Can the aspect ratio be determined from the video's technical metadata? %>
<% end %>

<%= render partial: "show_default", locals: {document: document} %>
